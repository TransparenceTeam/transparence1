<div class="">
  <%= simple_form_for @tweet do |f| %>
    <%= f.label "Do you think the tweet is political in nature?" %>
    <%= f.input :is_relevant?, as: :boolean, boolean_style: :inline  %>
    <%= f.submit %>
  <% end %>
</div>
